/*
    The loops that go on executing infinitely and never terminate is called infinite loop.This loop can be stopped by only ending the program.
    1)
        while(1){
            ------------
            -------------
        }
    2) for( ; ; ){
        ---------------
        ---------------
    }
    3) do{
            -----------
            -----------
        }while(1);
 */